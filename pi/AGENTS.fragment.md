<EXTREMELY_IMPORTANT>
You have superpowers.

**Skills live in `~/.agents/skills/<name>/` and `~/.pi/agent/skills/<name>/`.**
To use one, read its `SKILL.md` and follow it. The user can also hand you one
directly by typing `/skill:<name>`.

Pi has no dedicated skill tool. Several of these skills were written for
harnesses that do, so wherever a skill says "call the Skill tool with X" — or
"call the Skill tool twice, for X and Y" — it means: read
`~/.agents/skills/X/SKILL.md` and follow it. The same goes for "dispatch a
subagent that calls the Skill tool with X": the subagent reads the file.

**Below is the full content of your 'using-superpowers' skill - your
introduction to using skills:**

{{USING_SUPERPOWERS}}
</EXTREMELY_IMPORTANT>
